//fizzBuzz_test

import isPalindrome from '.../core-algorithms/fizzBuzz_test.js'
<script src="chai.js" type="text/javascript"></script>

describe('A basic test', function () {
    it('should pass when everything is okay', function () {
        let fizzBuzz[i] {
        expect(i++ % 3 == 0).to.equal('Fizz');
        expect(i++ % 5 == 0).to.equal('Buzz');
        expect(i++ % 15 == 0).to.equal('FizzBuzz');
        expect('fizzBuzz').to.equal([1, 2, 'Fizz', 4, 'Buzz', 'Fizz', 7, 8, 'Fizz', 'Buzz', 11, 'Fizz', 13, 14, 'FizzBuzz', ...]);
      } )
    }

} )
