//isPalindrome_test

import isPalindrome from '.../core-algorithms/isPalindrome_test.js'
<script src="chai.js" type="text/javascript"></script>
describe('A basic test', function () {
    it('should pass when everything is okay', function () {
        let isPalindrome(str) {
        expect(str).to.equal('radar');
        expect('radar').to.be.a('string');
        expect(revstr).to.be('radar');
        expect(true).to.be.true;
      })
      it('should pass when everything is okay', function () {
          let isPalindrome(str) {
          expect(str).to.equal('bananas');
          expect('bananas').to.be.a('string');
          expect(revstr).not.to.be('bananas');
          expect(false).to.be.false;
      })
      it('should pass when everything is okay', function () {
          let isPalindrome(str) {
          expect(str).to.equal('A man, a plan, a canal: Panama');
          expect('A man, a plan, a canal: Panama').to.be.a('string');
          expect(revstr).to.be('A man, a plan, a canal: Panama');
          expect(true).to.be.true;
        })
    } )

} )
